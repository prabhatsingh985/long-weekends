{
  "name": "thelongweekends",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "vitest run && astro build && wrangler pages deploy",
    "deploy:preview": "vitest run && astro build && wrangler pages deploy --branch=preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.3",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "vitest": "^4.1.11",
    "wrangler": "^4.125.0"
  }
}
